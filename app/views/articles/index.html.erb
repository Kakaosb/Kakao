<h1>Все статьи</h1>

<table>
  <tr>
    <th>Название</th>
    <th>Текст</th>
    <th>Автор</th>
    <th></th>
    <th></th>
  </tr>

<% @articles.each do |article| %>
  <tr>
    <td><%= article.title %></td>
    <td><%= article.text %></td>
    <td><%= article.user.nick%>




   </td>
    <td><%= link_to 'Показать', article %></td>
    <td><%= link_to 'Редактировать', edit_article_path(article) %></td>
    <td><%= link_to 'Уничтожить', article, method: :delete,
                                     data: { confirm: 'Вы уверенны?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новая статья', new_article_path %>